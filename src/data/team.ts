import type { TeamMember } from '@/types/content';

/**
 * Executive committee / team roster.
 * Populate this array when the Team page is implemented.
 */
export const teamMembers: TeamMember[] = [];
