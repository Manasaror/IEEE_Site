import type { EventItem } from '@/types/content';

/**
 * Workshops, events, and community initiatives.
 * Populate this array when event listings are implemented.
 */
export const events: EventItem[] = [];
