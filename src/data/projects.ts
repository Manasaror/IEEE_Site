import type { Project } from '@/types/content';

/**
 * Student projects and technical initiatives.
 * Populate this array when the Projects & Workshops page is implemented.
 */
export const projects: Project[] = [];
